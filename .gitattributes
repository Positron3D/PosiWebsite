# Normalize line endings: treat text as LF in the repo, auto on checkout.
* text=auto eol=lf

# Binary assets — never touch.
*.jpg   binary
*.jpeg  binary
*.png   binary
*.webp  binary
*.gif   binary
*.ico   binary
